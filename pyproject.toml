[project]
name = "causallift"
version = "0.1.0"
description = "Causal decisioning system for incrementality and treatment optimization."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false